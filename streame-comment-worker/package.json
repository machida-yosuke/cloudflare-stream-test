{
  "scripts": {
    "dev": "wrangler dev src/index.ts",
    "deploy": "wrangler deploy --minify src/index.ts",
    "generate": "drizzle-kit generate:sqlite",
    "migrate:local": "wrangler d1 migrations apply stream-comment --local",
    "migrate": "wrangler d1 migrations apply stream-comment"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.1.11",
    "hono": "^3.12.6",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20231218.0",
    "drizzle-kit": "^0.20.13",
    "drizzle-orm": "^0.29.3",
    "drizzle-zod": "^0.5.1",
    "typescript": "^5.0.4",
    "wrangler": "^3.22.0"
  }
}
